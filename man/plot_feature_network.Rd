% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_by_network.R
\name{plot_feature_network}
\alias{plot_feature_network}
\title{Plot feature network connectivity}
\usage{
plot_feature_network(
  network_metrics,
  feature_names = NULL,
  top_n = 10,
  color_by = c("degree", "strength")
)
}
\arguments{
\item{network_metrics}{Output from \code{\link{analyze_feature_network}}}

\item{feature_names}{Character vector of feature names (must match network_metrics)}

\item{top_n}{Number of top-connected features to label. Default is 10.}

\item{color_by}{Degree centrality (connections) or strength (connection weight).
Default is "degree".}
}
\value{
A ggplot object (edge list format for flexibility) or null plot
        if igraph is not available.
}
\description{
Creates a visualization of the feature network showing connections between
features. Useful for exploring ecological relationships and identifying
}
\examples{
if (requireNamespace("igraph", quietly = TRUE)) {
  data(example_feature_table)
  abund <- as.matrix(example_feature_table[, -1])
  similarity <- abs(cor(t(abund)))
  diag(similarity) <- 0
  net <- analyze_feature_network(similarity)
  plot_feature_network(net)
}
}
