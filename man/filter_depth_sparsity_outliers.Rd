% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_depth_sparsity.R
\name{filter_depth_sparsity_outliers}
\alias{filter_depth_sparsity_outliers}
\title{Convenience function: filter samples based on depth-sparsity outliers}
\usage{
filter_depth_sparsity_outliers(
  table,
  metric = c("sparsity", "richness"),
  outlier_method = c("mad", "iqr", "both"),
  multiplier = 3,
  direction = c("high_sparsity", "low_sparsity", "both"),
  keep_outliers = FALSE
)
}
\arguments{
\item{table}{A feature table (data.frame or matrix)}

\item{metric}{What to analyze: \code{"sparsity"} or \code{"richness"}}

\item{outlier_method}{Method: \code{"mad"}, \code{"iqr"}, or \code{"both"}}

\item{multiplier}{MAD multiplier for outlier detection}

\item{direction}{Which outliers to filter: \code{"high_sparsity"}, \code{"low_sparsity"},
or \code{"both"}. Default is \code{"high_sparsity"}.}

\item{keep_outliers}{Logical. If TRUE, keep outliers; if FALSE (default), remove them.}
}
\value{
Filtered feature table (same structure as input)
}
\description{
Combines \code{\link{analyze_depth_sparsity}} with filtering to remove
flagged outlier samples in one step.
}
\examples{
data(example_feature_table)
result <- filter_depth_sparsity_outliers(example_feature_table)
ncol(result)
}
