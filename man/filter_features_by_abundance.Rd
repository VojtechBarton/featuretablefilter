% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_features.R
\name{filter_features_by_abundance}
\alias{filter_features_by_abundance}
\title{Filter features based on abundance threshold}
\usage{
filter_features_by_abundance(
  table,
  threshold,
  mode = c("absolute", "relative"),
  remove_zeros = TRUE,
  min_samples = 1
)
}
\arguments{
\item{table}{A feature table (data.frame or matrix) with features as rows and samples as columns.
First column should be feature IDs, remaining columns are sample counts/abundances.}

\item{threshold}{Minimum abundance threshold. Features with values below this will be set to zero.}

\item{mode}{Filtering mode: "absolute" for raw counts, "relative" for proportions/percentages.
Default is "absolute".}

\item{remove_zeros}{Logical. If TRUE (default), remove features that become all zeros after filtering.}

\item{min_samples}{Optional. Minimum number of samples where a feature must exceed the threshold
to be kept. Default is 1 (feature must exceed threshold in at least one sample).}
}
\value{
A filtered feature table (data.frame) with low-abundance features zeroed and/or removed.
}
\description{
Sets low-abundance features to zero and removes features that are all zeros.
Works with both absolute counts and relative abundances.
}
\examples{
data(example_feature_table)
result <- filter_features_by_abundance(example_feature_table, threshold = 10)
nrow(result)
}
