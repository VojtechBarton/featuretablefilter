% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_data_classes.R
\name{from_phyloseq}
\alias{from_phyloseq}
\title{Convert from phyloseq object to feature table data.frame}
\usage{
from_phyloseq(phylo_obj, transpose = TRUE, include_taxa = FALSE)
}
\arguments{
\item{phylo_obj}{A phyloseq object containing at least an OTU table.}

\item{transpose}{Logical. If TRUE (default), transposes so features are rows and samples are columns.
phyloseq stores OTU tables with features as rows by default, but some operations
may change this.}

\item{include_taxa}{Logical. If TRUE and tax_table is present in phyloseq, appends taxonomy
information as additional columns after the feature ID.}
}
\value{
A data.frame with feature IDs in the first column, followed by sample counts.
        If include_taxa=TRUE, taxonomy columns are appended.
}
\description{
Extracts the OTU table from a phyloseq object and converts it to the standard
data.frame format used by this package (feature IDs in first column, samples as columns).
}
\examples{
# if (requireNamespace("phyloseq", quietly = TRUE)) {
#   ps <- phyloseq::phyloseq(
#     phyloseq::otu_table(matrix(rpois(100, 5), 10, 10), taxa_are_rows = TRUE),
#     phyloseq::tax_table(matrix(sample(c("A","B","C"), 100, replace=TRUE), 10, 10))
#   )
#   df <- from_phyloseq(ps)
# }
}
